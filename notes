Notes:

live-server and nodmon keeps track of the changes
transpiler - transforms source code (es6) into target language(es5).
Stateless functional components = constant function, one that will always return the same component(e.g. HTML).  Will not have any methods.
Stateful component(class) - one that is dynamic and can change.



https://medium.com/@dan_abramov/smart-and-dumb-components-7ca2f9a7c7d0#.x0thifaf9




// The this keyword and .call, .bind methods...

// var obj1 = {
//   a: 123,
//   b: 456
// };

// var someFunc = function() {
//   return this.a + this.b;
// };


// someFunc.call(obj1);

// var obj1BoundtosomeFunc = someFunc.bind(obj1);
// var obj2BoundtosomeFunc = someFunc.bind(obj2);
// var obj3BoundtosomeFunc = someFunc.bind(obj3);

//props is a way of passing values from a parent to a child in React

Events:
1) Listener - detect change
2) Trigger - change
3) Response/Action - Click Handler


Fetch/Jquery Ajax request are easier ways to make a XMLHttp request.


API is the way to interact with a server
api vs scraping
endpoint - what we want (complete url)
